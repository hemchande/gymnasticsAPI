# Gymnastics Analytics Backend Requirements - Python 3.8 Compatible
# Core web framework
Flask>=2.0.0,<3.0.0
Flask-CORS>=3.0.0,<4.0.0

# Database
pymongo>=4.0.0,<5.0.0

# Computer Vision and Machine Learning
opencv-python>=4.5.0,<5.0.0
# Note: opencv-contrib-python conflicts with opencv-python, use one or the other
# opencv-contrib-python>=4.5.0,<5.0.0

# MediaPipe - compatible with Python 3.8
mediapipe>=0.8.0,<1.0.0
numpy>=1.19.0,<2.0.0
scipy>=1.7.0,<2.0.0

# Additional dependencies for MediaPipe
protobuf>=3.17.0,<4.0.0

# Image Processing
Pillow>=8.0.0,<11.0.0
matplotlib>=3.3.0,<4.0.0

# HTTP Requests
requests>=2.25.0,<3.0.0

# Production Server
gunicorn>=20.0.0,<22.0.0

# Environment Management
python-dotenv>=0.19.0,<2.0.0

# Data Processing
# Note: These are typically included with Python, but listed for completeness
# json - built-in
# datetime - built-in
# os - built-in
# subprocess - built-in
# threading - built-in
# tempfile - built-in
# shutil - built-in
# base64 - built-in
# math - built-in
# time - built-in
# typing - built-in (Python 3.5+)
# urllib.parse - built-in
# bson - comes with pymongo
# logging - built-in
# argparse - built-in
# collections - built-in
# pathlib - built-in (Python 3.4+)
# http.server - built-in
# socketserver - built-in
# sys - built-in
# queue - built-in
# gc - built-in
# csv - built-in
# dataclasses - built-in (Python 3.7+)

# Development and Testing (optional - uncomment if needed)
# pytest==7.4.3
# pytest-flask==1.3.0

# Production Server (choose one based on deployment target)
# gunicorn==21.2.0  # For Linux/macOS production
# waitress==2.1.2   # For Windows production
# uwsgi==2.0.23     # Alternative production server

# Environment Management (optional)
# python-dotenv==1.0.0

# Additional utilities that might be useful
# psutil==5.9.6     # System monitoring
# schedule==1.2.0   # Task scheduling
